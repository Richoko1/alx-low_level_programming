#include "main.h"

/**
* add - Entry point for the addition
* @l: one value to be added together
* @m: second value to be added
* Return: Always 0 (Success)
*/

int add(int l, int m)

{
return (l + m);
}
